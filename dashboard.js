async function load(){const r=await fetch('/api/author/data');if(r.status===401){location.href='login.html';return}const j=await r.json();document.getElementById('registrations').innerHTML=j.registrations.map(x=>`<article class="card"><span class="eyebrow">${x.status}</span><h2 style="font:700 2rem 'Barlow Condensed'">${x.id}</h2><p><strong>${x.participant_name}</strong><br>${x.participant_email}<br>${x.participant_phone}</p><p style="color:var(--muted)">${JSON.parse(x.events_json).join(' · ')}</p><small>${x.created_at}</small></article>`).join('')||'<div class="card">No registrations yet.</div>';document.getElementById('tickets').innerHTML=j.tickets.map(x=>`<article class="card"><span class="eyebrow">${x.status}</span><h2 style="font:700 2rem 'Barlow Condensed'">${x.id}</h2><p><strong>${x.subject}</strong><br>${x.name} · ${x.email}</p><p style="color:var(--muted)">${x.message}</p></article>`).join('')||'<div class="card">No help tickets yet.</div>'}document.getElementById('logout').onclick=async()=>{await fetch('/api/auth/logout',{method:'POST'});location.href='login.html'};load();
