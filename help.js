document.getElementById('helpForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target),payload=Object.fromEntries(f.entries());const r=await fetch('/help',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();helpMsg.textContent=j.ok?`Request received. Your ticket ID is ${j.ticket_id}. Keep it for follow-up.`:(j.error||'Unable to send request.');if(j.ok)e.target.reset();});