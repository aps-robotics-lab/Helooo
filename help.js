document.getElementById('helpForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await submitJSON('/api/help',Object.fromEntries(f.entries()));e.currentTarget.classList.add('hidden');const m=document.getElementById('helpMessage');m.className='notice';m.innerHTML=`Ticket created successfully.<br><strong>${r.id}</strong><br>Save this reference to check progress.`;m.classList.remove('hidden')}catch(err){const m=document.getElementById('helpMessage');m.className='error';m.textContent=err.message;m.classList.remove('hidden')}});document.getElementById('trackBtn')?.addEventListener('click',async()=>{const id=document.getElementById('ticketId').value.trim();if(!id)return;const r=await fetch('/api/help/'+encodeURIComponent(id));const j=await r.json();document.getElementById('trackResult').innerHTML=r.ok?`<div class="notice"><strong>${j.id}</strong><br>Status: ${j.status}<br>${j.reply?`Reply: ${j.reply}`:'Our team will review your request and contact you soon.'}</div>`:`<div class="error">${j.error||'Ticket not found.'}</div>`})
